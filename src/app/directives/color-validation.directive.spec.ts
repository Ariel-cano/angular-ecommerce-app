import { ColorValidationDirective } from './color-validation.directive';

describe('ColorValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorValidationDirective();
    expect(directive).toBeTruthy();
  });
});
